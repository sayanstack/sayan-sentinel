# Public (non-secret) API base URL for the hosted deployment. Not a secret —
# committed so Vercel's build always points at the right backend without
# needing a separate dashboard env var to stay in sync with this repo.
NEXT_PUBLIC_API_URL=https://api-production-10bce.up.railway.app
