# @sayan-sentinel/ui

Shared React component library (shadcn/ui-based) used by apps/web.

**Status:** not implemented yet — see [../../docs/implementation-plan.md](../../docs/implementation-plan.md). `apps/web` currently keeps its handful of UI components (`Sidebar`, `StatCard`, `ErrorBanner`, `NotImplementedPage`) inline under its own `src/components/` since there's only one consumer so far; extracting a shared package makes sense once a second app needs them.
