# vulnerable-demo-app

Intentionally vulnerable local fixture (broken object authorization, unsafe input handling, fake test secrets, a reproducible vulnerable dependency) used by Sentinel's local demo mode. Never contains real credentials; dynamic validation may only target it with LOCAL_LAB_MODE=true.

**Status:** not implemented yet — see [../../docs/implementation-plan.md](../../docs/implementation-plan.md).
